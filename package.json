{
  "name": "past-paper-course",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix exam-system install && npm --prefix exam-system run build && node scripts/mirror-next-output.mjs"
  },
  "dependencies": {
    "next": "16.2.3"
  }
}
